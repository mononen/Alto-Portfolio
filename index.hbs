{{!< default}}

<div class="content-area">
    <main class="site-main">
        <div class="post-feed">
            {{#foreach posts}}
                {{> "loop"}}
            {{/foreach}}
        </div>
    </main>
</div>